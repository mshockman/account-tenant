import {Box, Button} from "@mui/material";

export function RealmSettings(
{
    id
}: {
    id: string;
}) {
    return (
        <Box>
            <Button onClick={() => alert("Not yet implemented")}>Delete Realm Placeholder</Button>
        </Box>
    )
}